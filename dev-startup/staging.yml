version: "3"
services:
  nginx-proxy:
    image: jwilder/nginx-proxy:alpine
    ports:
      - "80:80"
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
  wordpress:
    depends_on:
      - mysql
    image: wordpress:latest
    deploy:
      replicas: 5
      placement:
        constraints: []
    environment:
      VIRTUAL_HOST: wp.deweixiang.com
      WORDPRESS_DB_PASSWORD: "mypass_user"
      WORDPRESS_DB_USER: "myuser"
      WORDPRESS_DB_NAME: "wordpressDB1"
      WORDPRESS_DB_HOST: mysql.deweixiang.com:3306
    depends_on:
      - mysql
    ports:
      - "8080"
    networks:
      - webnet
  mysql:
    image: mysql:5.7
    volumes:
       - db_data:/var/lib/mysql
    environment:
      VIRTUAL_HOST: mysql.deweixiang.com
      MYSQL_ROOT_PASSWORD: "root_password"
      MYSQL_DATABASE: "wordpressDB1"
      MYSQL_USER: "myuser"
      MYSQL_PASSWORD: "mypass_user"
    ports:
      - 3306:3306
    deploy:
      placement:
        constraints: [node.role == manager]
    networks:
      - webnet
  notebook:
    image: jupyter/tensorflow-notebook
    environment:
      VIRTUAL_HOST: notebook.deweixiang.com
    deploy:
      placement:
        constraints: [node.role == manager]
    networks:
      - webnet
  web:
    # replace username/repo:tag with your name and image details
    image: microsoft/dotnet-samples:aspnetapp
    deploy:
      replicas: 1
      placement:
        constraints: [node.role == manager]
    environment:
      VIRTUAL_HOST: web.deweixiang.com
    ports:
      - "8080"
    networks:
      - webnet
  visualizer:
    image: dockersamples/visualizer:stable
    ports:
      - "8080"
    environment:
      VIRTUAL_HOST: status.deweixiang.com
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
    deploy:
      placement:
        constraints: [node.role == manager]
    networks:
      - webnet

networks:
  webnet:
volumes:
    db_data: